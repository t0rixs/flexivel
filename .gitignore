# ── バックエンド（フロントのみデプロイのため除外） ──
backend/

# ── サービスアカウント・秘密鍵 ──
*service-account*.json
*.p12
*.pem

# ── 環境変数 ──
.env
.env.*

# ── Flutter / Dart ──
frontend/.dart_tool/
frontend/build/
frontend/.flutter-plugins
frontend/.flutter-plugins-dependencies
frontend/.packages
frontend/pubspec.lock

# ── ビルド成果物 ──
**/libflutter.so
**/*.apk
**/*.aab
**/*.ipa

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.idea/
.vscode/
*.sublime-workspace
*.iml

# ── Makefile（ローカル開発用） ──
Makefile

# ── その他 ──
*.log
.temp/
.tmp/

# ── Docker（バックエンド用） ──
Dockerfile
.dockerignore
docs/zenn_flexivel_article.md
